<h1>My tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li>
    <input type="checkbox" <%= "checked" unless task.completed %>>
    <%= link_to task.title, task_path(task) %>
  </li>
  <% end %>
</ul>

<p><%= link_to '> add new task', new_task_path() %></p>
